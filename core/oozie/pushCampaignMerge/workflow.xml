<workflow-app xmlns="uri:oozie:workflow:0.5" name="dap-pushCampaignMerge-wf">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queueName}</value>
            </property>
        </configuration>
    </global>

    <start to='pushCampaignMerge' />

    <action name="pushCampaignMerge">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <exec>run.pl</exec>
            <argument>-t</argument>
            <argument>${jobEnv}</argument>
            <argument>-c</argument>
            <argument>pushCampaignMerge</argument>
            <env-var>YARN_CONF_DIR=/etc/hadoop/conf</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>${scriptHome}/run.pl#run.pl</file>
            <capture-output/>
        </shell>
        <ok to="pushCampaignFtpUpload"/>
        <error to="fail"/>
    </action>

    <action name="pushCampaignFtpUpload">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <exec>ftp_upload.pl</exec>
            <argument>-t</argument>
            <argument>${jobEnv}</argument>
            <argument>-c</argument>
            <argument>campaigns</argument>
            <env-var>YARN_CONF_DIR=/etc/hadoop/conf</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>${scriptHome}/ftp_upload.pl#ftp_upload.pl</file>
            <capture-output/>
        </shell>
        <ok to="mobilePushCampaignQuality"/>
        <error to="fail"/>
    </action>

    <action name="mobilePushCampaignQuality">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <exec>run.pl</exec>
            <argument>-t</argument>
            <argument>${jobEnv}</argument>
            <argument>-c</argument>
            <argument>mobilePushCampaignQuality</argument>
            <env-var>YARN_CONF_DIR=/etc/hadoop/conf</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>${scriptHome}/run.pl#run.pl</file>
            <capture-output/>
        </shell>
        <ok to="end"/>
        <error to="fail"/>
    </action>

    <kill name="fail">
        <message>Workflow failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>

    <end name="end" />

</workflow-app>