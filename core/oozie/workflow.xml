<workflow-app xmlns="uri:oozie:workflow:0.3" name="dap-wf">
    <start to='bobAcqFull1' />

    <action name="bobAcqFull1">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
            <exec>run.pl</exec>
            <argument>-t</argument>
            <argument>test-prod</argument>
            <argument>-c</argument>
            <argument>bobFull</argument>
            <env-var>YARN_CONF_DIR=/etc/hadoop/conf</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>scripts/run.pl#run.pl</file>
            <capture-output/>
        </shell>
        <ok to="end"/>
        <error to="fail"/>
    </action>

    <kill name="fail">
        <message>Something went wrong: ${wf:errorCode('bobAcqFull1')}</message>
    </kill>

    <end name='end' />
</workflow-app>